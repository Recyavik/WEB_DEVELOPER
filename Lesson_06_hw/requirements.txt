# Зависимости проекта — список Python-пакетов, которые нужно установить
# pip install -r requirements.txt установит все пакеты из этого файла
# Версии зафиксированы (==), чтобы у всех разработчиков был одинаковый результат

fastapi==0.135.3          # Главный фреймворк для создания веб-API и веб-приложений
uvicorn[standard]==0.44.0 # Веб-сервер ASGI для запуска FastAPI; [standard] включает дополнительные возможности
jinja2==3.1.6             # Шаблонизатор: подставляет данные Python в HTML-файлы
python-multipart==0.0.24  # Обработка данных HTML-форм (Form(...) в FastAPI требует этот пакет)
sqlalchemy==2.0.36        # ORM-библиотека: позволяет работать с базой данных через Python-объекты
psycopg2-binary==2.9.10   # Драйвер для подключения к PostgreSQL из Python; binary — без компиляции из исходников
