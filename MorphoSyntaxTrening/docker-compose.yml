services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - db_data:/app/instance
      - ./static:/app/static
      - ./templates:/app/templates
    env_file:
      - .env
    restart: unless-stopped
    dns:
      - 8.8.8.8
      - 8.8.4.4

volumes:
  db_data:
